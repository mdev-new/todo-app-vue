export enum SERVER_STATUS {
  SUCCESS = "success",
  FAIL = "fail"
}

export enum SERVER_STATUS_EXTRA {
  NOT_FOUND = "not_found",
  INVALID_INPUT = "invalid",
  ABORTED = "aborted",
  ALREADY_EXISTS = "already_exists"
}
